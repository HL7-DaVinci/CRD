package org.hl7.davinci.endpoint.components;

import java.util.List;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class CdsConnectTest {
  @Test
  public void testExecute() {
    /* TODO: implement a test that does not depend on an instance of CDS Connect running
    CdsConnect cdsConnect = new CdsConnect("http://cdsc-test.dd:8083");
    List<String> rules = cdsConnect.getRules("cms", "82947", "cpt");
    assertNotNull(rules);
    assertEquals(1, rules.size());
    List<String> rules2 = cdsConnect.getRules("cms", "94660", "cpt");
    assertNotNull(rules);
    assertEquals(1, rules2.size());
    List<String> rules3 = cdsConnect.getRules("cms", "", "cpt");
    assertNotNull(rules);
    assertEquals(2, rules3.size());
    List<String> rules4 = cdsConnect.getRules("crd", "56778", "cpt");
    assertNotNull(rules);
    assertEquals(0, rules4.size());
    */
  }
}
